name: app_core
description: Shared non-UI core utilities, error abstractions, formatters, and environment configuration.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.15.0
  intl: ^0.20.2

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
