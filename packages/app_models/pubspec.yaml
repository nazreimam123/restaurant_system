name: app_models
description: Pure Dart enums, domain models, and DTOs for the QR restaurant ordering platform.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
